import { RegistrationComponent } from './registration.component';
import { Routes } from '@angular/router';

export const RegistrationPages = [RegistrationComponent];

export const RegistrationRoutes: Routes = [{ path: 'registration', component: RegistrationComponent }];
