import { ApprovalQueueResolver } from './approval-queue.resolver';
import { ApproveContentResolver } from './approve-content.resolver';

export const DashboardResolvers = [
    ApprovalQueueResolver,
    ApproveContentResolver,
];