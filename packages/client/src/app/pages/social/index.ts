import { Routes } from '@angular/router';

import { SocialComponent } from './social.component';

export const SocialPages = [SocialComponent];

export const SocialRoutes: Routes = [{ path: 'social', component: SocialComponent }];
